from django.urls import path

app_name = 'payments'

urlpatterns = [
    # Payment endpoints will be added based on gateway integration
    # path('razorpay/create/', ...),
    # path('razorpay/verify/', ...),
    # path('stripe/create/', ...),
    # path('stripe/verify/', ...),
]
